apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: todo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      name: backend-pod
      namespace: todo-app
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: venisasarah/todo-backend:v1.0.2
        ports:
        - containerPort: 8000
      