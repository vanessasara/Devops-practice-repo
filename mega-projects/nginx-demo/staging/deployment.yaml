apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: nginx-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-demo-staging
  template:
    metadata:
      labels:
        app: nginx-demo-staging
    spec:
      containers:
        - name: nginx
          image: ghcr.io/nginxinc/nginx-unprivileged:latest
          ports:
            - containerPort: 80
