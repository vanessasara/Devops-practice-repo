apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      name: frontend-pod
      namespace: todo-app
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: venisasarah/todo-frontend:v1.0.1
        ports:
        - containerPort: 3000
        
